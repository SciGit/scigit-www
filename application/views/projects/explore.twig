{% extends "base.twig" %}

{% block title "Explore" %}

{% block content %}

<div class="page-header">
  <p class="breadcrumbs">
    {{ anchor("/projects", "Projects") | raw }} <span class="divider">&gt;</span>
    <span class="current">{{ anchor("/projects/explore", "Explore") | raw }}</span>
  </p>
</div>

<div class="row-fluid">
  <div class="span6 well-list">
    <table class="table">
      <tr>
        <th>
          <h1>Popular Projects</h1>
        </th>
      </tr>
      {% for project in projects %}
        <tr>
          <td class="project" valign="middle" data-proj_id="{{ project.id }}">
            {{ anchor("/projects/view/#{project.id}", project.name) | raw }}
            <div class="buttons">
              <div class="button">
                <a href="#" rel="tooltip" title="Subscribers" class="icon-rawlink">
                  {{ project.member_count }}
                  <i class="icon-star"></i>
                </a>
              </div>
            </div>
            <div class="description">
              {% if project.description %}
                {{ wordwrap(project.description, 50) }}
              {% else %}
                No description.
              {% endif %}
            </div>
          </td>
        </tr>
      {% endfor %}
    </table>
  </div>

  <div class="span6 blue-well-list">
    <table class="table">
      <tr>
        <th>
          <h1>Featured Projects</h1>
        </th>
      </tr>
      {% if featured_projects %}
        {# XXX: This should really be templated #}
        {% for project in featured_projects %}
          <tr>
            <td class="project" valign="middle" data-proj_id="{{ project.id }}">
              {{ anchor("/projects/view/#{project.id}", project.name) | raw }}
              <div class="buttons">
                <div class="button">
                  <a href="#" rel="tooltip" title="Subscribers" class="icon-rawlink">
                    {{ project.member_count }}
                    <i class="icon-star"></i>
                  </a>
                </div>
              </div>
              <div class="description">{{ wordwrap(project.description, 50) }}</div>
            </td>
          </tr>
        {% endfor %}
      {% else %}
        <tr class="nohover">
          <td valign="middle">
            <p>There are currently no featured projects.</p>
          </td>
        </tr>
      {% endif %}
    </table>
  </div>
</div>

{% endblock %}
