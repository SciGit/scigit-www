{% extends "base.twig" %}

{% block title %}
My Projects
{% endblock %}

{% block content %}

<div class="page-header">
	<h1>
		My Projects
		<a href="{{ site_url('/projects/create') | raw }}" class="btn title-btn">
			<i class="icon-plus"></i>
			Create Project
		</a>
	</h1>
</div>

<table class="table table-bordered">
	{% for project in projects %}
		<tr>
			<td valign="middle">
				{{ anchor("/projects/changes/#{project.id}", project.name) | raw }}
				{% if project.can_admin %}
					<a href="{{ site_url("/projects/admin/#{project.id}", 'Admin') | raw }}" class="btn-mini" style="float: right">
						<i class="icon-wrench"></i>
						Admin
					</a>
				{% endif %}
			</td>
		</tr>
	{% endfor %}
</table>

{% endblock %}
